<Window x:Class="GuttyRL.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GuttyRL"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="GUTTYTECH · RL OPTIMIZER"
        Width="1180"
        Height="760"
        MinWidth="960"
        MinHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        StateChanged="OnWindowStateChanged">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource BorderStrongBrush}"
            BorderThickness="1"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="OnTitleBarMouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="18,0,0,0"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                IsHitTestVisible="False">
                        <Border Width="8"
                                Height="8"
                                Margin="0,0,10,0"
                                CornerRadius="4"
                                Background="{DynamicResource CtaBrush}" />
                        <TextBlock FontFamily="{DynamicResource TitleFontFamily}"
                                   FontSize="17"
                                   Text="GUTTYTECH  /  RL OPTIMIZER"
                                   VerticalAlignment="Center" />
                        <TextBlock Margin="12,1,0,0"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   Text="{Binding VersionLabel}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="MinimizeButton"
                                Style="{DynamicResource CaptionButtonStyle}"
                                ToolTip="Minimizar"
                                Click="OnMinimizeClick"
                                Focusable="False">
                            <Path Width="10"
                                  Height="1"
                                  Stretch="Fill"
                                  Fill="{DynamicResource TextPrimaryBrush}"
                                  Data="M0,0 H10" />
                        </Button>
                        <Button x:Name="MaximizeButton"
                                Style="{DynamicResource CaptionButtonStyle}"
                                ToolTip="Maximizar"
                                Click="OnMaximizeClick"
                                Focusable="False">
                            <Path x:Name="MaximizeIcon"
                                  Width="10"
                                  Height="10"
                                  Stretch="Uniform"
                                  Stroke="{DynamicResource TextPrimaryBrush}"
                                  StrokeThickness="1.2"
                                  Fill="Transparent"
                                  Data="M1,1 H9 V9 H1 Z" />
                        </Button>
                        <Button Style="{DynamicResource CloseCaptionButtonStyle}"
                                ToolTip="Fechar"
                                Click="OnCloseClick"
                                Focusable="False">
                            <Path Width="10"
                                  Height="10"
                                  Stretch="Uniform"
                                  Stroke="{DynamicResource TextPrimaryBrush}"
                                  StrokeThickness="1.2"
                                  Data="M1,1 L9,9 M9,1 L1,9" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <local:OptimizerPanel Grid.Row="1" />
        </Grid>
    </Border>
</Window>
